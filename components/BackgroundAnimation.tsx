import React from 'react';

const BackgroundAnimation: React.FC = () => {
  // This component is disabled for a cleaner, more professional look.
  // A subtle background is now handled by CSS in index.html.
  return null;
};

export default BackgroundAnimation;